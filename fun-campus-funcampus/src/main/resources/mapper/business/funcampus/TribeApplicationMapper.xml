<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.akkkka.admin.module.business.funcampus.tribe.dao.TribeApplicationDao">

    <!-- 查询结果列 -->
    <sql id="base_columns">
        tribe_application.id,
        tribe_application.tribe_id,
        tribe.name AS tribe_name,
        tribe_application.portal_user_id,
        tribe_application.username,
        tribe_application.reason,
        tribe_application.status,
        tribe_application.review_remark,
        tribe_application.review_user_id,
        tribe_application.review_time,
        tribe_application.create_time
    </sql>

    <!-- 管理端：申请分页查询 -->
    <select id="queryPage" resultType="com.akkkka.admin.module.business.funcampus.tribe.domain.vo.TribeApplicationVO">
        SELECT
        <include refid="base_columns"/>
        FROM tribe_application
        LEFT JOIN tribe ON tribe.id = tribe_application.tribe_id
        WHERE tribe_application.deleted_flag = 0
        <if test="queryForm.tribeId != null">
            AND tribe_application.tribe_id = #{queryForm.tribeId}
        </if>
        <if test="queryForm.status != null">
            AND tribe_application.status = #{queryForm.status}
        </if>
        ORDER BY tribe_application.id DESC
    </select>

    <!-- 门户：我的申请列表 -->
    <select id="queryMyList" resultType="com.akkkka.admin.module.business.funcampus.tribe.domain.vo.TribeApplicationVO">
        SELECT
        <include refid="base_columns"/>
        FROM tribe_application
        LEFT JOIN tribe ON tribe.id = tribe_application.tribe_id
        WHERE tribe_application.deleted_flag = 0
        AND tribe_application.portal_user_id = #{portalUserId}
        ORDER BY tribe_application.id DESC
    </select>

</mapper>
