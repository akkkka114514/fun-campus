# 关于活动开始延后功能的想法

先交代下背景。活动有报名开始/结束、活动开始/结束、签到开始/结束几组时间点,状态全靠定时任务翻。一开始是每个活动单独建一个定时任务,活动多了内存顶不住,听 ai 的建议改成统一扫描;后来嫌全表扫还是浪费,干脆每天把"当天会到期"的活动塞进 redis,扫描任务只从 redis 拿 id,再回数据库改状态。查询量是下来了,但埋了个雷:redis 和数据库分家,两边得想办法保持一致。

这个雷是这么踩出来的。活动可能因为不可抗力要推迟开始时间,要修改活动时间表里的开始时间、redis 里当天到期的集合。

麻烦在哪呢。定时任务扫描是一轮一轮把 redis 里的 id 捞进内存再挨个处理的,我先把活动从 redis 删了也没用,它这一轮手里已经攥着 id 了,照样会把状态改成"进行中",删 redis 只能拦住下一轮。ai 提了个醒,这种状态变更别用无脑 update,要让数据库自己兜底:

```sql
UPDATE activity SET status = '进行中'
WHERE id = ? AND status = '待开始' AND start_time <= NOW();
```

条件不满足就影响 0 行,白跑一趟,出不了事。推迟动作也照这个思路来,顺手把状态重置回"待开始":

```sql
UPDATE activity SET start_time = ?, status = '待开始'
WHERE id = ? AND status = '待开始' AND start_time = ? AND start_time > NOW();
```

条件里其实塞了两个意思:一是旧开始时间必须在未来,只有还没开始的活动才允许推迟;二是拿旧开始时间当条件,两个管理员同时点推迟,后提交那个条件不满足直接失败,等于乐观锁。影响 0 行就提示冲突,不用上分布式锁那么重的东西。

想通这点,redis 和 mysql 就没必要强一致了,最终一致就够。redis 删早了、删晚了、甚至漏删都无所谓,到点的时候数据库条件满足了才会真改,其它情况全是空跑。

操作顺序倒是有个坑。我一开始写的是"先删 redis,再改 mysql",后来觉得不对:万一 redis 删了,mysql 事务还没提交,进程崩了,活动当天就不会被扫到,数据库里还是旧开始时间;第二天重建 redis 只收当天到期的,旧时间早过了也不会再收进去,活动就永远卡在"待开始",还没人会发现,最恶心的静默故障。所以顺序反过来,先提交 mysql 事务,再去删/改 redis。就算崩在中间,redis 残留个旧条目,到点条件不满足空跑一次,不影响,第二天按新时间重建就正常。

还有个容易漏的细节:推迟完如果开始时间还在当天,redis 那条不能删,得更新它的到期时间,只有推迟到别的天才删。这种多时间点的活动,当天集合里可能同时有好几条记录(报名、开始、签到各一条),处理哪几条得看业务口径。

下面记几个业务上还没想清楚的问题:

1. 推迟是只挪活动开始时间,还是整条时间线一起挪?只挪开始的话活动时长就缩水了,报名、签到这些窗口跟不跟着动?这直接决定 redis 里要动哪些条目。
2. 我前面写的场景是"进行中的活动因为不可抗力退出来推迟",后面想的又是"还没开始的活动推迟",这其实是两码事。进行中被打断应该是先暂停,之后恢复,要处理的是剩余时长和结束时间,状态机跟"未开始顺延"完全不一样,得拆成两个用例做。
3. 推迟的时候报名可能已经截止了,要不要重开?报名进行中的要不要一起顺延?
4. 要不要通知已经报名的人?

另外我打算给推迟动作加个申请审批。隐患是流程走的时间一长,活动可能已经到点自动开始了,等批下来再执行推迟,"还没开始"这个前提就不成立了。所以审批通过真正执行时要重新校验一遍状态和时间,推迟本身也要做成幂等的,重复提交也不怕。

redis 说到底只是个索引,最终一致想睡得着觉,还得加个低频对账任务兜底,扫两类矛盾:一是"待开始但开始时间已经过了"的,就是上面说的卡死场景,发现就补触发或者告警;二是"进行中但开始时间还在未来"的,说明被误置了,要回滚状态。redis 那边每天重建时整体覆盖当天的 key,别追加,免得旧条目一直残留。

漏洞：当天新建活动当天到达时间要修改某个状态，会错过大定时任务。

1、如果活动不在redis里，也就是说这个活动不是当天的要改状态，直接操作数据库修改activity schedule即可。
2、如果活动在redis里，现在要推迟这个当天要到达的时间，则删除redis。
考虑不是立即要到达













首先，我想做功能的扩大。只要是根据时间改状态，都用大小循环处理。然后就是活动可以延长当前时间段的时间。然后就是开始设计延后申请表功能。



一、两个 Job 的代码问题
任务推进拿 Redis"当天活动名单"当处理依据——ScanJob 没跑、缓存过期、当天新建活动漏进名单，状态就停更，而且不能自愈（这轮讨论的核心）
帮我把大循环开关功能删掉。缓存过期时间你来说处理方法。当天新建活动能否通过判断是否当天开启活动来手动加redis记录。

t_smart_job 里 job_class 还是旧包名 net.lab1024.，扫描/推进任务实际从没执行过（已修）
这个你处理

原来 UpdateJob 配每天 0 点跑一次，天级粒度撑不起报名准入的需求（enroll 是 status=1 硬校验，报名开始/截止当天全乱）
这个你处理

原查询条件 status != 4 会把待审核(9)的活动误推进（改回双任务后的草稿里又带回来了，待修）
这个你处理

时间字段为 null 时的 NPE 没有防护；单条异常没隔离，一条炸全批（草稿版丢了，待重加）
这个你处理

ScanJob 旧实现：limit 不分页、逐个活动查时间表（N+1）、状态用魔法数字、直接改 enabled_flag 动态启停任务（绕过框架还得等刷新周期生效）
启停功能删掉，其他你处理

ScanJob 时间区间 [00:00:00, 23:59:59] 收尾有边界洞，应改半开区间；它的缓存该不该带 generatedAt（新鲜度）也还没定
我不知道怎么弄，跟我讲讲

UpdateJob 的防倒退保护（时间表后调时不让状态倒退）在草稿版里丢了，待重加
我想要可以倒退，然后倒退后把报名数据清空，之后重新报名。
当前代码现状：ScanJob 编译不过（getActivitiesWithKeyTime 被删）+ 没有 t_smart_job 注册记录，部署了也不会

草稿版 UpdateJob 是全表分页扫 status≠4 再用名单过滤——全表查一次没省掉，双任务收益没兑现；名单为空直接 return，ScanJob 一挂就永远空转
把代码先给我看一下

Redis 拿出来的 List 强转 (List<Long>) 有反序列化类型风险（可能是 Integer）
帮我处理

二、状态机这边
时间表整体后调 → 期望状态小于当前状态，Job 会把已推进的状态"倒回去"（防倒退保护的由来）
"结束后才批准延长"和防倒退天然冲突，只能走"受控倒退"（审批流程显式写状态），不能放宽 Job 逻辑；v1 建议直接禁止这种情况
已触发下游（完结审核、奖励结算）的活动再往后延，会有"复活"一类的状态冲突，没细聊
三、Redis 和 MySQL 一致性（这次讨论的主角）
病根是把名单当成了"事实"（决定处理谁）；讨论定的方向是缓存只当提示、库才是事实，miss/超过新鲜度就回源重建——所有误差合流到一条自愈路径
写入端（新建活动、批准延期）要不要检查 ScanJob 死活：结论是不查，无条件尽力投递，失败吞掉，"感知死活"只留在 UpdateJob 读缓存时判断
动态启停和回源兜底互相冲突（被禁用的任务永远不跑、没机会自愈），建议砍掉启停
延期批准的一致性：MySQL 事务内完成时间表+状态重算，提交后再动 Redis；Redis 操作不进事务、失败不影响审批；真正要防的是批准时时间表被暗改（快照比对）
操作顺序的坑（你笔记里写过）：先删 Redis 再改库，中途崩溃会导致活动永远卡在待开始 → 必须先提交 MySQL 再动 Redis
四、延后申请功能，业务上还没定的
推迟范围：只挪活动开始时间（时长缩水），还是整条时间线一起挪？（直接决定 Redis 里动哪些条目）
"进行中被打断" vs "还没开始顺延"是两个用例，状态机完全不同，得拆开做
报名已经截止/进行中的，要不要重开或一起顺延？
批下来时活动可能已经自动开始了，执行推迟要重新校验状态+时间、做成幂等（你笔记里提过）
当天新建、当天就要改时间的活动会错过 ScanJob（你笔记里那个"漏洞"，规则 1/2 已经想了一半，还有"不是立即到达"的情况没落）
要不要通知已报名的人；以及现有缺口：updateActivityWithSchedule 还是空实现、发布后没有正规的改表通道（审核期改表被 validateAllTimeFuture 卡在"报名未开始"）