# 学生端前端技术方案

后端门户端（portal）接口这两天我盘了一遍：登录、首页、活动详情、报名、签到码、评论、收藏、分享、订单支付、消息、文件上传这些其实都已经就位了，部落和学分认定还是半成品或空白。所以这份方案不光是"前端怎么搭"，也得把"哪些接口能直接用、哪些得先补后端"一起说清楚，不然页面画完了照样联调不动。

范围按 README 的 Phase 2~7 全量做：首页、活动、报名、签到签退、我的、部落、消息、学分认定、日历。

---

## 选型和项目位置

定了 uni-app，那就把话说全：Vue3 + Vite 版模板（`uni-preset-vue#vite`），语言先用 JS 不上 TS——理由是跟现有 smart-admin-web 的写法和习惯保持一致，一个人的项目上 TS 收益有限，折腾编译配置的时间不如拿去补后端。如果后面面试想讲 TS 再加也行，但别在收尾阶段给自己加戏。

- 状态管理：pinia（uni-app Vue3 官方支持），只放用户态和 token，别把列表数据塞进去
- UI 库：推荐 uview-plus。组件覆盖全（Calendar、Upload、Picker、表单校验这些都是这个项目实打实要用的），Vue3 兼容成熟，中文文档。备选 uni-ui（官方但组件朴素）、wot-design-uni（好看但 TS 优先）。先用 uview-plus，个别组件不满意再单独换，别混用三套
- 项目名沿用之前叫过的 `fun-campus-app`，放在仓库根目录，跟 smart-admin-web 平行

开发姿势：日常用 CLI（`npm run dev:h5`）在浏览器里调，热更新快、还能直接看网络面板；HBuilderX 只在两个场景用——真机运行、云打包 App。小程序端这期不做，但别写死只有 H5 的逻辑，扫码那块留好条件编译的口子。

tabBar 四个：首页 / 部落 / 消息 / 我的。tabBar 图标不支持字体图标，得准备静态 png（先用简单图标占位，别在这卡住）。

## 请求层和登录

不给 uni-app 装 axios，直接封装 `uni.request` 成一个 promise 版 request.js，行为对齐 smart-admin-web 的 axios.js，别让两边手感不一致：

- baseURL 从环境变量取：`.env.development` 里 `VITE_APP_API_URL=http://127.0.0.1:1024`，H5 直连后端（CORS 后端已开）；真机调试换成局域网 IP，就这一个地方要改
- 每个请求带 `Authorization: Bearer <token>`，token 用 `uni.setStorageSync` 存
- 响应里 `code !== 1` 统一 toast；`30007/30008`（登录失效）清 token 踢到登录页
- GET 走 params、POST 走 body，别搞混——后端有一批接口是 `@RequestBody Long activityId` 这种裸参形式（比如报名、收藏），传的时候 body 直接就是 `123` 这种裸数字，不是对象，这个坑第一次联调肯定会踩

登录流程：

1. `GET /portal/login/getCaptcha` → 拿到 `captchaUuid` + `captchaBase64Image`（直接可以当 img src 用）+ `expireSeconds`
2. `POST /portal/login` 带 `username / password / captchaCode / captchaUuid / loginDevice`
   - `loginDevice`：H5 传 4，安卓 App 传 2，iOS 传 3，小程序传 5
   - 密码目前后端那行解密是注释掉的，直接明文传；等哪天后端启用 apiEncrypt 再改，代码里留个 TODO
3. 返回 `token` 存本地，跳首页；失败就直接显示后端 msg（后端中文提示挺全的，别在前端自作主张翻译）

要注意：登录返回的信息很有限（token、id、username、userType 那些），"我的"页面要的学校、学院、积分（`gradeScore`、`creditScore`）不在里面。所以还缺一个"当前用户信息"接口，见下面的缺口清单。

## 扫码这块拆成三种情况说

这是之前最容易糊涂的地方，一次讲清楚：

**一、学生出示签到码（本期主流程）。** `GET /portal/activityEnrollment/signIn/QRCode` 有个好消息：后端直接返回 ZXing 生成的 PNG base64（300x300），前端 `<image :src>` 一贴就完事，配个 30 秒倒计时到期重新请求。前端不需要引任何二维码库，也不需要 canvas 画码。码内容是 `userId+token`，谁扫？签到员——用管理后台那个 `signin-qrcode.vue` 调试页扫，然后选活动提交。也就是说签到的"动作"是签到员做的，学生端只负责亮码。一人一码、不绑活动，签到签退成功后码作废。

**二、学生端"扫一扫"能干什么。** 现在后端没有"活动码"接口，能扫的只有分享码：`GET /portal/activity/share/resolve/{token}`（免登录）→ 拿到 activityId → 跳详情。首页那个扫码按钮就干这个。想要"学生扫活动码直接签到"那种体验，得新增一套活动码接口，建议这期不做，别把签到流程搞出两条路。

**三、各端扫码能力别想当然。** `uni.scanCode` 在 App 和小程序上是原生能力，H5 端不支持。H5 调试环境要么用 `html5-qrcode` 之类的库走摄像头（需要 HTTPS 或 localhost），要么 H5 端干脆不显示扫码入口，从分享链接进。方案上留条件编译：App/小程序走 `uni.scanCode`，H5 走库降级，这个后面写代码时再验证一遍。

## 支付这块的回查要单独说

付费活动链路后端已经全做完了（下单锁座 → 模拟收银台 → 回调写报名）。前端要接的就三下：

1. 详情页报名按钮分流：免费调 `enroll`，付费调 `POST /portal/activityOrder/create` 拿订单
2. 拉起收银台：`GET /portal/activityOrder/prepay?orderNo=xxx` 拿支付参数，mock 渠道下收银台就是后端的 HTML 页 `/portal/payment/mock/cashier?orderNo=xxx`。H5 直接 `window.open`，App/小程序用 `web-view` 包一层打开
3. 关键点：支付成功是渠道异步回调，不是收银台同步返回的。用户在收银台点了支付回到 App，前端并不知道结果，得轮询 `GET /portal/activityOrder/detail?orderNo=` 直到状态变成已支付或已关闭（15 分钟超时关单）。做个 2~3 秒一次、最多轮个 30 秒的轮询，别做成死循环

活动详情接口里已经带了 `currentUserOrder` 字段（当前用户在活动下的最新订单），详情页可以直接展示"待支付/已支付/已退款"状态，这省了单独查订单的活。

## 页面清单（对着 README Phase 2~7 排）

页面路径按 pages.json 的约定来，先说清单和依赖：

| 页面 | 依赖接口 | 后端状态 |
|------|---------|---------|
| 登录页 | `/portal/login/getCaptcha`、`/portal/login` | 就绪 |
| 首页（本校/全局切换、搜索、扫码入口、消息入口） | `GET /portal/homeData`（`activeActivityPage=1本校/2全局`） | 就绪。搜索/分类筛选后端没有参数，一期先用前端过滤，数据量大了再补 |
| 活动详情（信息、倒计时、报名人数、成员、附件、富文本简介） | `GET /portal/activity/detail`、`GET /portal/activity/countdown` | 就绪 |
| 详情页的报名/收藏/分享/评论 | `enroll` / `favorite` / `share` / `comment` 系列 | 就绪 |
| 支付确认页 + 收银台跳转 + 回查 | `activityOrder/create`、`prepay`、`detail` | 就绪 |
| 我的-签到码页（个人二维码 30s 刷新） | `GET /portal/activityEnrollment/signIn/QRCode` | 就绪 |
| 我的订单（待支付/已支付/退款 Tab） | `POST /portal/activityOrder/query`、`cancel`、`refundApply` | 就绪 |
| 消息中心（列表、未读角标、已读） | `/portal/message/queryMyMessage`、`getUnreadCount`、`read/{id}` | 就绪（自动消息的生成见缺口 10） |
| 我的页（资料、积分、入口） | `GET /portal/portalUser/current` | 已补（缺口 1） |
| 我的活动（已报名/已签到/已完成） | `POST /portal/activityEnrollment/queryMy` | 已补（缺口 2） |
| 待签到/待签退待办 | 我的报名 + 时间窗口过滤 | 已补（缺口 2、4，detail 带 `currentUserEnrollment`） |
| 资料编辑（头像、昵称、手机号） | `POST /portal/portalUser/updateProfile`（上传已有 folder=5 头像） | 已补（缺口 5） |
| 报名范围校验提示（学院/年级/部落） | detail 里 `canEnrollCollege/Grade/Tribe` 已有，规则判定在后端 | 就绪（前端只展示提示文案） |
| 部落列表/详情/成员/加入申请 | `POST /portal/tribe/queryPage`、`GET /portal/tribe/detail`、`member/queryPage`、`activity/queryPage`、`application/apply`、`application/myList` | 已补（缺口 6~9） |
| 学分认定列表/详情/申请 | `POST /portal/creditApplication/apply`、`GET /portal/creditApplication/detail/{id}`、`GET /portal/creditApplication/myList` | 已补（缺口 11） |
| 活动日历 | `GET /portal/activity/calendar`（startDate/endDate/activeActivityPage） | 已补（缺口 13） |
| 待评价（Phase 4.3） | `POST /portal/activityEvaluation/submit`、`GET /portal/activityEvaluation/pending/list`、`pending/count`、`mine` | 已补（缺口 12） |

评论页的坑提前记一笔：发表评论前先看 `ActivityCommentQueryForm` 的字段和后端排序参数，热门/最新两种排序要区分开（后端有 `/activity/comment/hot` 单独接口）。

## 后端缺口清单

> **状态（已更新）**：以下 13 项后端缺口已全部补齐，条目保留原始描述供追溯，行尾「已补」为实际落地接口。
> SQL 均已合并进 `sql_script/mysql/fun-campus-merged.sql`：新增 `tribe_application`、`activity_evaluation`、`credit_application` 三张表，`tribe` 表新增 `icon`/`description` 字段，并追加了“学分认定”菜单与按钮权限（`creditApplication:query`/`creditApplication:review`）。已建库环境重新执行该脚本或择段执行即可。

按"卡不卡主流程"分两档。

**卡主流程的（不补前端就得写 hack）：**

1. **当前用户信息**。加 `GET /portal/portalUser/current` 之类的接口，返回学校/学院/年级名称、`gradeScore`、`creditScore`、头像、手机号。现在管理端那个 `/backend/portalUser/queryPage` 学生用不了（会被 AdminInterceptor 拒）。参考写法：照抄 detail 接口里拼 `ActivityVO` 归属名的 `getNameById` 思路
   已补：`GET /portal/portalUser/current`（返回学校/学院/年级名称、`gradeScore`、`creditScore`、头像、手机号）
2. **我的报名分页**。`activityEnrollment` 表按 `user_id` 查询，支持状态筛选（待审核/已通过/已签到/已完成）。Service 里目前只有活动维度的查询，要按用户维度加一个，顺带把活动标题/封面/时间表 join 出来，不然前端要 N+1 地查详情
   已补：`POST /portal/activityEnrollment/queryMy`（联表带活动标题/封面/时间）
3. **取消报名**。表有 `deletedFlag` 可以逻辑删，但付费活动要联动退款或按 `refund_policy` 拒绝，免费活动要 `decreaseEnrollNum`。这块别图快，照着现有 `deleteDraft` 的事务风格写
   已补：`POST /portal/activityEnrollment/cancel`（付费联动退款、免费回收名额、CAS 防并发）
4. **活动详情补 `currentUserEnrollment` 字段**。现在 detail 里只有全部报名者列表和当前用户订单，没有"我报名了没、签到没"的直接字段。前端从 `enrollUsers` 里找自己不可靠（分页、隐私都是问题）。加一个字段最省事，免费付费通用
   已补：详情 VO 新增 `currentUserEnrollment` 字段（未报名时为 null）
5. **更新个人资料**。`POST /portal/portalUser/updateProfile`（头像 file key、手机号、性别；用户名和学校学院不建议开放改）
   已补：`POST /portal/portalUser/updateProfile`（仅开放头像 fileKey、手机号、性别）

**不卡主流程但全量范围绕不开的：**

6. **部落字段与门户接口**。`tribe` 表现在只有 name/category/president/belong_to/school_id，Phase 5.1 要的图标、简介、热度字段都没有。建议先想清楚部落这期做到什么程度：列表 + 详情 + 成员列表（`tribe_user` 表是现成的）先做，图标/简介这期加上，评分、收藏、公告这些建议二期
   已补（一期）：`tribe` 表新增 `icon`、`description` 字段；评分/收藏/公告仍留二期
7. **加入部落申请**。Phase 5.3 那张表单（理由、兴趣特长、志愿选择、是否服从调剂）现在没有任何表支撑，`tribe_user` 只是一张关系表。要么建申请-审核表，要么简化成"直接加入"。这是产品决策，建议简化成一期版本
   已补（一期简化版）：建 `tribe_application` 表（理由 + 审核），门户 `POST /portal/tribe/application/apply`、`GET /portal/tribe/application/myList`；管理端 `POST /backend/tribeApplication/queryPage`、`POST /backend/tribeApplication/review`
8. **部落门户接口**。`TribeService` 的管理端 CRUD 是现成的，但门户侧只有 `query/simple`。需要：部落分页列表、详情、成员列表、部落发起的活动列表
   已补：`POST /portal/tribe/queryPage`、`GET /portal/tribe/detail?tribeId=`、`POST /portal/tribe/member/queryPage`、`POST /portal/tribe/activity/queryPage`
9. **TribeController 前后缀**。这个模块接口路径要统一成门户 `/portal/tribe/*` 和管理端 `/backend/tribe/*` 两套，别混着来（README Phase 0.3 里有记录）
   已补：管理端统一为 `/backend/tribe/*`（前端 `tribe-api.js` 已同步）
10. **消息的发送接入**。接口全有，但活动状态变更、审核结果、支付成功这些业务节点还没调 `MessageService.sendMessage`。这个不卡学生端——消息页面能显示，只是没有自动消息进来。排队尾补
   已核实：支付成功/退款/审核结果等节点已接入消息发送，无需追加改动
11. **学分认定从零建**。表（`credit_application`）、CRUD、审核流、状态机都没有。这是 Phase 6.2 里最重的一块，相当于一个小模块
   已补：`credit_application` 表 + 门户 6 接口（apply/update/delete/detail/myList/reviewer/query，待审核可编辑可删）+ 管理端 2 接口（queryPage/review，权限点 `creditApplication:query`/`creditApplication:review`），状态机 0 待审 → 1 通过 / 2 驳回
12. **评价（Phase 4.3 待评价）**。后端完全没有表。建议直接砍掉或标记"规划中"，别为了凑页面从零建
   已补（未砍）：`activity_evaluation` 表 + 门户 6 接口（submit/queryByActivity/queryMy/mine/pending/list/pending/count）；规则：已报名 + 已签到 + 活动已结束才可评价，一人一活动一条
13. **日历查询**。缺按日期查活动的接口。一期可以先用 `homeData` 全量拉 + 前端按 schedule 日期归类凑合，数据量大了再说
   已补：`GET /portal/activity/calendar?startDate=&endDate=&activeActivityPage=`（1 本校 / 2 全局，区间跨度 ≤62 天）

我的建议是把 1~5 作为前置（前端做完核心页面之前补掉），6~9 跟部落页面并行做，10~13 按优先级排后面。砍不砍 11、12 你来定，但这两个是纯增量工程，加起来比 Phase 2~4 的页面工作量还大。

## 目录结构

```
fun-campus-app/
├── src/
│   ├── api/                 # 一个模块一个文件，命名对齐 smart-admin-web
│   │   ├── login-api.js
│   │   ├── activity-api.js
│   │   ├── enrollment-api.js
│   │   ├── order-api.js
│   │   ├── comment-api.js
│   │   ├── message-api.js
│   │   └── ...
│   ├── components/          # 活动卡片、倒计时、评论项这些业务组件
│   ├── pages/               # 页面目录，跟 pages.json 对应
│   │   ├── login/
│   │   ├── index/           # 首页
│   │   ├── activity/        # 详情、支付确认
│   │   ├── order/           # 订单列表、详情
│   │   ├── signin/          # 我的签到码
│   │   ├── tribe/
│   │   ├── message/
│   │   └── mine/
│   ├── store/               # pinia：user store（token、用户信息）
│   ├── utils/
│   │   ├── request.js       # uni.request 封装
│   │   └── auth.js          # token 读写、跳登录
│   ├── static/              # tabBar 图标、占位图
│   ├── App.vue
│   ├── main.js
│   ├── pages.json
│   ├── manifest.json
│   └── uni.scss
├── .env.development
├── .env.production
└── package.json
```

## 开发顺序

按依赖排的，每步做完都是能跑能看的状态：

1. **脚手架 + 请求封装 + 登录 + tabBar 骨架**。跑通 `getCaptcha → login → token → 带 token 请求 homeData`，这一步把请求层的手感定下来，后面全依赖它
2. **首页 + 活动详情 + 免费报名**。核心浏览链路，`homeData` 双 tab 分页、详情页全量展示、倒计时组件、报名按钮状态机（未报名/已报名/报名截止）
3. **付费链路**。报名分流、下单、收银台拉起、轮询回查。联调用 mock 渠道全流程走一遍：下单 → 收银台点成功/失败 → 回查状态，失败和超时关单也各试一次
4. **签到码 + 扫码 + 消息中心**。亮码页（倒计时刷新）、分享码扫码入口、消息三个接口加上未读角标
5. **后端的"我的"四件套补完后，接我的页、我的活动、待办、资料编辑**
6. **部落（边补后端边做）、日历**。学分认定看取舍再排

每步联调的前提：后端本地 1024 端口跑着，H5 dev 直连。13 项后端缺口已全部补齐（见上），前端可直接按顺序开发。

## 几个容易踩的坑

- **所有请求必须以 `/portal/` 开头**。AdminInterceptor 是靠 URL 里有没有 `portal`/`backend` 路径段来分派用户体系的，不带这俩段的请求直接拒。新增接口、改路径的时候都别忘了这茬，mock 收银台那种后端 HTML 页也是同理
- `activity/detail` 没挂 `@NoNeedLogin`，未登录看详情会被拦。分享链接进来先引导登录再跳转
- 密码目前明文传输（后端解密代码是注释状态），别自己在前端先加密，否则必然登录失败
- 文件上传用 `uni.uploadFile`，`/portal/file/upload`，folder 传 5（头像）；记得 header 里手动带 Authorization，uploadFile 不走 request 封装
- 倒计时组件（`/activity/countdown`）返回的是 `remainingSeconds`，本地每秒递减就行，归零了重新请求拿下一阶段，别每秒打接口
- mock 收银台是 `@ConditionalOnProperty(pay.channel=mock)` 注册的，生产环境不存在，前端判断支付渠道时别写死依赖它
- 后端一堆接口是 `@RequestBody` 裸 Long（报名、收藏、分享生成这些），body 是裸数字不是对象，postRequest 封装时注意别统一强转成 JSON 对象

## 最后

这份方案里"缺"的接口，一半可以在 fun-campus-funcampus 现有模块里加方法就完事（1~5、8），另一半（部落申请、学分认定）是从零建表建模块，工作量别低估。开工顺序建议严格按上面的 1→6 来，第 1、2、3 步做完就是一个能演示的完整闭环了——面试讲的时候，扫码亮码、支付异步回查、token 双端隔离这几个点都是现成的素材，不用刻意准备。
