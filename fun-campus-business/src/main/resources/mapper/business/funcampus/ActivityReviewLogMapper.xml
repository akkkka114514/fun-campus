<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.akkkka.admin.module.business.funcampus.activityReviewLog.dao.ActivityReviewLogDao">

    <!-- 查询结果列 -->
    <sql id="base_columns">
        activity_review_log.id,
        activity_review_log.activity_id,
        activity_review_log.reviewer_id,
        activity_review_log.reviewer_name,
        activity_review_log.review_stage,
        activity_review_log.action,
        activity_review_log.reject_reason,
        activity_review_log.check_remark,
        activity_review_log.create_time
    </sql>

    <!-- 分页查询 -->
    <select id="queryPage" resultType="com.akkkka.admin.module.business.funcampus.activityReviewLog.domain.vo.ActivityReviewLogVO">
        SELECT
        <include refid="base_columns"/>
        FROM activity_review_log
    </select>


</mapper>
