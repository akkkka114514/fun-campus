<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.business.funcampus.collegeInfo.dao.CollegeInfoDao">

    <!-- 查询结果列 -->
    <sql id="base_columns">
        college_info.id,
        college_info.name,
        college_info.school_id,
        college_info.deleted_flag,
        college_info.create_time,
        college_info.update_time
    </sql>

    <!-- 分页查询 -->
    <select id="queryPage" resultType="net.lab1024.sa.admin.module.business.funcampus.collegeInfo.domain.vo.CollegeInfoVO">
        SELECT
        <include refid="base_columns"/>
        FROM college_info
    </select>

    <update id="batchUpdateDeleted">
        update college_info set deleted_flag = #{deletedFlag}
        where id in
        <foreach collection="idList" open="(" close=")" separator="," item="item">
            #{item}
        </foreach>
    </update>

    <update id="updateDeleted">
        update college_info set deleted_flag = #{deletedFlag}
        where id = #{id}
    </update>

    <select id="getCollegeInfoByUserId" resultType="net.lab1024.sa.admin.module.business.funcampus.collegeInfo.domain.vo.SimpleCollegeInfoVO">
        select id,name
        from college_info
        where school_id =
              (select school_id
               from backend_user
               where id = #{userId})
    </select>

</mapper>
