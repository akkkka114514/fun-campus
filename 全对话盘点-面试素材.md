# 全对话盘点-面试素材

这份东西的来历：把这段时间所有对话过一次筛子——从最早的"支付能不能用 sleep 模拟"到最近的"写死问题总结"，聊过的东西全摊开来，凡是面试能开口讲的都留下。

跟另外几份文档的分工：`面试冲刺计划.md` 是排期加主线话术；`写死问题-面试讲法.md` 是写死这个专项的讲法；`关于活动开始延后功能的想法.md` 是延后功能的设计底稿。这份是素材仓库——聊过什么、代码现在真实长什么样、这块能讲到什么深度，一条条对着来。有些地方我跟冲刺计划写的不一样，以这份为准（冲刺计划是 9/19 写的，中间代码又动过好几轮）。

时间：状态全部核实到 9 月 24 日。只写真实状态，不带"计划中的样子"。

---

## 一、先对一遍家底（数字都核过）

技术栈：Java 21、Spring Boot 3.5.4、MyBatis-Plus 3.5.12、MySQL、Redis；权限 Sa-Token；审核流转评估过 Alibaba COLA 状态机；前端 Vue3 + smart-admin 那套管理后台。

规模：57 个 Controller（框架自带 10 个，业务侧 47 个，其中 funcampus 业务模块 22 个）；库里 31 张业务表外加 34 张框架表；测试 12 个测试类、178 个用例全绿（纯 Mockito 不是 SpringBootTest）。

已经闭环能演示的：活动全链路（草稿提交→多级审核→报名→签到签退→详情/倒计时）、活动范围控制（学院/年级/部落）、评论/点赞/撤销、收藏、分享、报名和审核结果站内信、付费链路代码（今天刚落地，还没测）。

规划里没做的：延后申请（只有设计）、学分认定、移动端；管理后台一堆收尾（34 个页面 import、菜单 SQL 路径、空壳 Controller、双端前缀适配）——这些细项在写死那份文档里。

工作区现在挂着 43 处未提交改动加一批没入库的新文件，面试前得处理掉（文末清单）。

---

## 二、所有聊过的主题，一块一块过

### 1. 活动状态推进：最值钱的故事，没有之一

聊过什么：两个 Job 的重写（改成 smart-admin 框架风格）、"大任务扫库放 Redis、小任务消费"这个方向到底对不对的辩论、笔记上一堆批注（半开区间、消费确认移除、倒退清报名）的落地、还有一次被你问"凌晨和深夜没人办活动，断档是不是无害"——你赢了一半，我认了半句：如果断档恰好落在凌晨、早上第一个切换点之前补上，确实不损失什么；但设计不能靠"恰好没人用"成立，而且深夜不是无人区，报名截止时间点在半夜、跨天活动都没排除掉。

现在代码的真实样子（跟冲刺计划写的已经不一样了）：
- 两个任务，配置在 t_smart_job 里：小任务（ActivityStatusUpdateJob）fixed_delay 60 秒，消费名单推进状态；大任务（ActivityStatusScanJob）fixed_delay 3600 秒，扫库重建名单。
- 缓存管理器（ActivityStatusCacheManager）是这套的核心：名单只做加速提示，数据库才是事实。名字带"今天生成日期"标记，不是当天生成的就视为过期，直接回源查库重建；名单缺失、过期，全部收敛到这一条自愈路径。写入端（新建活动、改时间表）尽力投递，失败吞异常，漏投由重建兜底。
- 推进用条件更新（updateStatusIfMatch：where id=? and status=旧状态），和业务并发改状态互不覆盖。
- 倒退：这个跟冲刺计划写反了——冲刺计划里是"防倒退，跳过加 warn"，你后来把它改成了"允许倒退，但受控"：期望状态小于当前状态时，回退状态 + 逻辑删该活动的报名数据 + 报名数清零，三件事在同一个事务里，先用原状态做条件更新，失败整体回滚、下一轮重试，倒退后报名窗口重开就能重新报名。
- 消费确认：处理妥当、并且当天已经没有未来关键时间点的活动，从名单里移除，后面轮次不再重复检查；处理失败的留在名单里下轮重试。
- 单条异常隔离，一条炸不了整批；时间字段为 null 返回 null 跳过；从 Redis 拿出来的元素可能是 Integer，统一转 Long。

面试怎么讲（建议按这个新版讲，别按冲刺计划里"防倒退"那版）：
> 我的活动状态不是查询时实时算的，是定时任务推的——因为状态不只在展示用，报名判窗、审核、列表筛选都读它，实时算等于规则到处复制；落库之后规则只有任务一个出口。一开始为了省查询量做成两级任务，但更新任务对 Redis 名单是硬依赖，扫描任务没跑、缓存过期，状态就静默停摆，而且不能自愈——这不是理论风险，真发生过：任务类名在库里注册成了迁移前的旧包名，扫描任务从头到尾没跑起来过，日志上还看不出来。后来我把正确性从缓存里拿出来了：名单只当提示，缺失或过期自动回源查库重建，所有异常都合流到这一条自愈路径；写入端尽力投递、失败不影响业务。更新用条件更新防并发覆盖。还有一个变化是倒退——管理员改期或者活动被打断，按新时间表算期望状态比当前小，我选择允许它退，但退的时候把报名数据在同一事务里清掉，退完报名窗口重开，这样状态和数据语义不打架。

被追问的几条都备好：为什么保留两级不直接扫库（收益账：名单把范围从全表缩到当天几十上百个，正确性不依赖它，纯粹是省；而且这套设计里少了它也就慢一点）；名单里的活动处理失败怎么办（留在名单，下轮重试，单条隔离）；倒退误清了报名怎么办（事务+条件更新保证要么都做要么都不做；真实误判场景就一个：管理员改期，这属于要人来做决定的事，可用告警补强）。

### 2. 审核状态机：一段"评估完没上车"的取舍，讲法要改

聊过什么：你问我"引入的状态机有点鸡肋，本来就是为了面试有的说"，我先说错了一轮（说全项目零使用），核实后纠正：审核模块里其实已经搭了半套。接着把状态机相关的代码全摘出来评估，最后结论是"审核够格用状态机，但现阶段不需要"——因为手写校验已经在真实链路里干活了。

现在代码的真实样子：ActivityReviewStateMachineBuilder 里有 7 条 externalTransition（草稿→初审、初审→审阅、初审退回、审阅→终审、终审→报名审核、报名审核→完结审核、终审退回），但动作是 System.out.println 占位；Controller 里的注入和 fireEvent 已经摘掉，留了一行注释写着"审核状态机已从主流程摘出，材料待评估"；主流程（ActivityReviewLogService）跑的还是手写校验：阶段顺序（order 必须前一个）、动作白名单（例：内容审核阶段只允许"通过"），每次流转落审核日志表。

面试怎么讲，两个选择，面试前得挑一个：
- 选择 A（保持现状，推荐现在这么讲）："我做过一轮评估，把六阶段流转、退回路径用 COLA 状态机声明出来对比手写校验，结论是审核这种多阶段、带退回、要留痕的场景够格上状态机，但当前这部分手写校验已经覆盖了核心规则（阶段顺序、动作白名单），状态机框架带来的收益主要在'流转规则集中'，属于重构级改动。我把它作为备选方案抽出来了，还没有切换到主流程——先保证真实链路稳定，等流程要变化（比如不同学院不同环节）再上，那时候状态机的价值才真正兑现。"这个讲法诚实、有判断力，追问"为什么不直接上"也接得住。
- 选择 B（真想讲"用了状态机"，那就得先把它接回去）：接回去至少要三步——把 println 换成真实动作（写审核日志、改活动状态）、事件定义理一遍（现在 Event 是 name 枚举，跟 Stage 的映射得对齐）、主流程改成 fireEvent 触发。工作量不大但涉及面不小，别赶着面试前动。
- 千万别按冲刺计划里那句"六阶段审核流转用 COLA 状态机声明、每个迁移挂 action 留痕"讲——跟代码对不上，面试官要是让你打开代码看就穿帮了。

### 3. 枚举与字典：数字本身没罪，被迫当三样用才有罪

聊过什么：一次全链路字典盘查（发现状态值域三套口径、活动流程跟状态机是断的、签退字段漏几处）→ 你问"给活动阶段标数字之外有没有更好的方案" → 定下按审核模块的枚举方式做"b+c 方案" → 落地 ActivityStatus 枚举化（含 @EnumValue 映射、顺序链、字典 SQL 对齐）→ 中途发现一处语义偏移（枚举化时把"排除已结束"改没了，靠翻 git 历史原来版本定位）→ 修复成 pendingTimeLineCodes。

现在代码的真实样子：ActivityStatus 是个完整形态——@EnumValue code 跟字典 ACTIVITY_STATUS 的 data_value 对齐、label 跟 data_label 对齐、TIME_LINE 显式顺序链（0~4）、isAfter/isBefore 代替比较 code 大小、pendingTimeLineCodes() 给定时任务当候选、fromCode 反查、非时间阶段状态调用 timeOrder() 直接抛异常防误用。签到/签退不设状态，交给时间窗口实时校验。遗留一处：review_stage 的数据库列注释还是老口径（写"4-完结审核"，枚举里 4 是报名审核、5 才是完结审核），要么改注释要么心里有数。

面试怎么讲：数字的三种角色——存储值、含义（标签）、顺序；含义只活在注释里、一份事实抄多份、顺序靠 code 大小比较，都是同一个病。我这块的正例是 ActivityStatus（顺序链显式写出来，禁止比 code），反例是审核阶段枚举（没有 @EnumValue，靠 name 映射，配错会静默错位）。顺带能聊一个 MyBatis-Plus 知识点：3.5.2 以后默认处理器会按枚举有没有 @EnumValue/IEnum 自动分流，没注解的走原生 name 映射——配错就是"存进去 0，读出来 null"这种静默故障。

### 4. 一致性原则：缓存只当提示，库才是事实

聊过什么：你那篇笔记里的核心讨论——Redis 和 MySQL 的一致性问题、先删缓存再改库的坑、操作顺序、对账兜底。当时定的原则现在全都进了状态推进的实现里。

当时聊出来的结论（可以原样拿去讲）：
- 病根是把名单当成了"事实"（决定处理谁）。方向：缓存只当提示，库才是事实，miss 或过期就回源重建——所有误差合流到一条自愈路径。
- 操作顺序：必须先提交 MySQL 事务再动 Redis。反过来的话，Redis 删了、MySQL 没提交、进程崩了，活动当天不会被扫到，第二天重建又不收过期时间点，活动永远卡在待开始——最恶心的静默故障。现在的方向下就算崩在中间，Redis 残留个旧条目，到点条件不满足空跑一次，不影响。
- 写入端不查扫描任务死活，无条件尽力投递，失败吞掉；"感知死活"只留在读缓存那一端判断（正好是回源重建那条路）。
- 对账是兜底：扫两类矛盾（"待开始但开始时间已过"补触发或告警；"进行中但开始时间在未来"回滚）。这条还没实现，是延后功能和后续运维的待办。
- 顺带一个细节：时间区间用半开区间 [今天0点, 明天0点)，别写 [00:00:00, 23:59:59]——LocalDateTime 精度到纳秒，23:59:59.100 两头不沾。

### 5. CAS 用在哪：三判据，不是哪都上

聊过什么：你直接问"是不是所有审核后流转审核状态都要 CAS"。核实后发现审核线当时的条件更新只有主键，没有"仍然未结束"这个前置条件。

结论（讲的时候按这个）：不是都要。判据三条——有并发竞争（用户重复点、任务和用户抢）、状态机式互斥（同一笔数据只允许一个流转生效）、能幂等重试（失败可以安全重来或跳过）。项目里的四个用例：报名名额原子加减（免费报名和下单锁座都靠它）；活动状态推进的条件更新；支付回调与关单抢同一个 CAS（谁先成功谁生效）；延后批准用旧时间表做条件（两个管理员同时改，后提交的失败，等于乐观锁）。审核线的现状是"读的时候校验了、写的时候没锁"，这正好能当"我知道它差在哪"的例子讲。

### 6. 签到签退：拿状态当时间窗口用，是个语义病

聊过什么：完善签到二维码那轮，发现老代码 signIn 要求活动状态是"报名中"，但时间表模型里签到窗口在活动进行/结束阶段——状态是"报名中"时窗口没开，窗口开了状态又不是"报名中"，签到根本闭不了环。

现在代码的真实样子：一人一码（userId + UUID，不绑活动）、Token 30 秒 Redis、重新生成覆盖、签到成功即作废；窗口判定替换状态判定（now 落在 [windowStart, windowEnd]，签退还要求 needSignOut）；补了 7 个边界用例（无时间表、窗口未开始、窗口已结束、未配置、不需要签退等）；顺手修了一个稳定复现的 flaky 测试（时间锚点问题）。签到不再是状态，所以状态枚举里 5~8 那批值被清掉了。

面试怎么讲：状态是给流程用的（谁在什么阶段、能不能推进），时间窗口是给时间用的；用状态代理窗口，两套语义迟早打架。这个"矛盾"故事冲刺计划里已经写好了，直接用。

### 7. 消息通知：从"三套残骸"到"学生能收到"

聊过什么：先盘出三套设施（站内信 / 公告 / 邮件）全是"移植框架时只搬了后端"，然后一鼓作气把学生消息链路打通：新建自助服务（查我的消息/未读/已读，严格按登录用户过滤防越权）、接收人查询（支持搜学生）、消息模板扩了一批、管理端三个硬伤修掉（URL 前缀、权限点菜单 SQL、接收人只能选后台用户）、前端枚举和组件补齐。再往后是把通知接进了业务：报名成功/失败即时发、报名审核结果批量发（通过/未通过两个模板）。

现在代码的真实样子：模板枚举从 1 个扩到 8 个（1000 订单审批 + 2000~2006：报名通过/未通过/成功/失败、支付成功、订单超时关闭、退款到账）；六个发送点全是"尽力而为"——try/catch + warn 日志，通知失败绝不阻断业务主流程；站内信先落库（t_message），顶栏铃铛和消息列表页都能看；公告后端完整、管理端前端页面还缺（在缺口清单里）；没有 WebSocket，全是拉取，实时性够用不是缺陷。

面试怎么讲：通知我是分层的——业务动作只管把消息写进 t_message（模板 + 接收人类型 + 变量），这是事实；推送渠道是另一层，以后接 WebSocket 或者 MQ 都不用动业务代码。为什么失败不阻断：报名这样的主流程不能因为站内信写失败就回滚，通知是旁路。被追问"通知丢了怎么办"：落库是同步的、丢的概率极低；真要强保证，是往"业务动作发事件、监听器落库"的事件化改造，这也是我记得里给这模块留的演进方向。

### 8. 付费链路：设计是从对话里来的，代码是今天刚落的

聊过什么：你问"买支付宝 API 麻烦，能不能用 sleep 模拟"——被说服放弃，定了"模拟异步回调"方案（模拟收银台 + 同一回调入口），写进 README Phase 9 的一百多行设计；后来还做过一轮前后端缺口比对。

现在代码的真实样子（今天下午刚落地，全是未提交的新文件）：activityOrder 模块 18 个文件（订单/退款实体、下单服务、超时关单任务、订单号工具）+ payment 包 11 个文件（PaymentChannel 接口、MockPaymentChannel、统一回调控制器、模拟收银台控制器、支付服务）——下单锁座、15 分钟超时关单、回调走 CAS 幂等、支付成功才写报名记录、和免费报名链路互斥（付费活动走订单）。pay.channel=mock 只在 dev/test 环境配。编译刚验过能过；测试没写、全链路没冒烟、都还没提交。

面试怎么讲：按冲刺计划里那套讲（Mock 为什么不用 sleep 的三条理由、CAS 幂等、关单和回调竞态），这套是从对话里打磨出来的，讲起来顺。但两个前提：第一，面试前把测试和冒烟补上（冲刺计划第二周本来就是这么排的）；第二，讲的时候别超出代码实际状态——"下单→支付→报名"主链路现在能讲"设计加实现"，退款那条链路建议对照代码再核一遍讲。

### 9. 单元测试与工程踩坑：178 个用例是怎么来的

聊过什么：你让我"给复杂的逻辑写单测，简单的不用"，从那轮起测试从 144 个一路加到 178 个；中间每次任务都在和 IDE 抢 target 目录（JDT 语言服务器和命令行 Maven 并发写编译产物，导致各种"找不到符号""类文件消失"的假故障）。

现在代码的真实样子：12 个测试类、178 个用例全绿。测试原则：纯 Mockito 不启 Spring（快、不依赖 MySQL/Redis），专挑规则密集的类（校验器、时间窗口、状态流转、权限）。

踩过的坑清单（面试里"你写测试踩过什么坑"直接能讲）：
- 同一个类注入两个同类型依赖时 @InjectMocks 会按类型乱注，改成 @BeforeEach 里显式构造；
- 纯 Mock 环境里 MyBatis-Plus 的 lambda 构造器会报"找不到表信息"（正常靠 Spring 启动时注册），写了个注册器在测试初始化时手动注册 TableInfo；
- TransactionStatus 的包名（在 org.springframework.transaction，不在 support 子包）；
- Lombok 对原始类型 boolean 生成的是 isXxx() 不是 getXxx()；
- IDE 编译产物污染：target 里的 class 是语言服务器生成的"带错误的产物"，症状是各种诡异的找不到符号/类——解法是 clean 重编译，判别方法是看 class 文件时间戳晚于 Maven 构建时间。这个坑前后撞了四五次，后来总结成一条经验：命令行构建和 IDE 别同时写同一份 target。

可以加一句自省（面试加分项）：我早期测试里把一些隐含假设固化进去了（比如"活动状态只到 4"），后来靠翻 git 历史才发现枚举化时改丢了原意——这让我意识到测试也会骗人，写测试时要盯"意图"，不是盯"现状"。

### 10. 工程治理：SQL、构建、定时任务框架

SQL 合并：19 个脚本合并成一个 fun-campus-merged.sql（3357 行），处理了编码/BOM、重复段、依赖顺序（t_menu 建表在前）、幂等；还配了交叉校验（数量对账 + 分段比对）。

Maven 资源过滤的坑：`@profiles.active@` 占位符一直没被替换，根因是父 pom 里第三个 resource 配置（filtering=false + 通配）覆盖了第二个（开着过滤的），导致 SpringBootTest 和启动都拿不到正确的 profile——这种"构建配置的静默覆盖"值得讲，因为排查靠的是对比 target/classes 里的实际产物，不是猜。

SmartJob 框架（聊过一次实现细节）：DB 驱动 + 多实例防重的三层（Redis 分布式锁 → last_execute_time 间隔检查 → 执行日志落库推进时间）+ 配置变更走 Redis 发布订阅广播。答"定时任务怎么防多实例重复执行"这道题时用这个，比背八股强。

写死专项：独立成篇了（`写死问题-面试讲法.md`），五个案例都在那。这轮复核的残留现状供你修的时候参考：AdminApplication 的 COMPONENT_SCAN 还是 `net.lab1024.sa`（这个是真雷）；四个环境 yaml 的 AOP pointcut 和 dev 日志级别还是旧包名；`ActivitySigninManagerMapper.xml` 的 namespace/resultType 还是旧包名；代码生成器的默认路径里也有旧路径（框架自带，影响小）；SQL 里一部分演示数据的字符串带旧包名（无害）。

### 11. 面试准备本身

聊过什么：冲刺计划的诞生（先盘家底再排三周）、GitHub 简介改写（从"仿 PU 口袋校园"改成独立开发声明 + 亮点）、"一个人写的校园二课活动系统……全链路都通了"那版文案、亮点挑选的"能被追问"原则。

两个提醒：
- README 第 13 行"前端我实在写不下去了"这句还在，散装清单里提过但没删——面试官真的会翻仓库。
- README 的进度表跟代码脱节了：消息通知和付费两行还写着"未开始"，实际都动了；数字类（Controller 数量）也变了。面试前对着这份文档把 README 过一遍。

---

## 三、冲刺计划之外的深挖追问预演

Q：为什么保留两级任务，不干脆每次都全量扫库？
> 我算过这笔账：名单把扫描范围从全表缩到当天有关键时间点的几十上百个活动，这是纯收益。关键是它不承担正确性——名单没了、过期了，回源重建就是全量查一次库，慢一点但结果一样。所以"省"建立在"坏不了"上面，这才敢留。

Q：名单里某个活动处理失败会怎样？
> 留在名单里，下一轮重试；单条异常被隔离，不影响同批其他活动。最坏情况是它一直失败一直重试，那就靠日志和告警去看了——但不会"烂在名单里没人管"。

Q：倒退为什么要把报名数据清掉？这动作听着挺重的。
> 因为状态和数据要对得上。活动从"报名结束"退回到"报名中"，原来那批报名是旧时间表下产生的，不清的话，一场活动会有两批语义不同的报名数据。清掉之后报名窗口重开，大家重新报——这是跟"活动改期"这个业务动作对齐的。整个动作在一个事务里，先条件更新再清理，失败整体回滚、下轮重试，不会出现"状态退了数据没退"的中间态。

Q：时间区间为什么要半开？
> [00:00:00, 23:59:59] 收尾有洞：LocalDateTime 精度到纳秒，23:59:59.100 这种时间点，左边够不着右边也不算。改成 [今天0点, 明天0点) 之后，"今天"的定义严丝合缝，跟"明天"也不重不漏。

Q：通知系统你怎么设计的？为什么不用 WebSocket？
> 分两层：业务动作把消息写进 t_message（模板、接收人类型、变量），这是事实层；推送是渠道层，现在全靠拉取（顶栏铃铛 + 列表页），校园场景对实时性要求不高，拉取够了。要接实时推送的时候加渠道就行，业务代码不用动。没有为了显得高级去上 WebSocket。

Q：状态机的事，为什么最后没换到主流程？
> 评估过，不是拍脑袋。审核是个"六阶段、带退回、要留痕"的流程，够格用状态机；但当前手写校验已经覆盖了最核心的规则（阶段顺序、动作白名单），状态机的收益主要在流转规则集中——那是个重构级切换，收益跟风险比划不来。我把它抽出来留着了，等流程真的要变（比如不同学院不同环节）再上，那时价值才兑现。判断一个技术要不要上，我看的是"它解决的痛我疼不疼"，不是"它先不先进"。

Q：你怎么在没有 Spring 容器的测试里保证行为可信？
> 两个支点：一是被测类本身不依赖容器（构造器注入，测试里显式 new）；二是把容器帮忙干的初始化补上（MyBatis-Plus 的 TableInfo 手动注册）。剩下的就是 mock 的纪律——只 mock 边界（库、Redis、外部服务），业务规则真的跑。这样才敢说"我没测 Spring，但我测了规则"。

Q：开发环境里 IDE 和 Maven 打架那次，怎么定位的？
> 症状是"同一份代码，命令行报找不到符号，重跑又好了"。方法是拿证据链对时间戳：class 文件的写入时间晚于 Maven 构建结束时间，说明有别的进程在写 target；再看进程列表，找到不是我们启动的构建进程和语言服务器。结论是两边并发写编译产物。解法是构建前 clean、别在 IDE 编译的同时跑命令行构建。这类"环境性故障"看着玄学，其实都有时间戳可查。

Q：付费这块的难点在哪？还没测你就敢讲？
> 难点不在支付 API 本身，在幂等和竞态：回调可能重复投递、关单和回调可能同时发生、退款可能失败重试。判据就一条——所有订单状态变更都是"where status=旧状态"的条件更新，影响行数说话，重复来多少次都没有副作用。测试的话，冲刺计划里就排了这周补，讲之前我会把它测掉——没测的东西我不会说"已经完成"。

---

## 四、面试前收尾清单（从这轮盘点里拉出来的）

1. AdminApplication 包名残留先修掉——一行的事，但面试官翻代码，这是最扎眼的雷。顺带把四个 yaml 的 pointcut、mapper XML 的旧包名一起清了。
2. README 更新：删"前端我实在写不下去了"那句；进度表里消息通知、付费两行改成真实状态。
3. 付费代码：补测试（订单状态流转、重复回调、关单竞态、退款规则）+ 全链路冒烟，然后跟其他改动一起提交。
4. 状态机材料：决定是"接回主流程"还是"保持抽出待评估"，面试前要有明确说法，别只留个注释在那；顺手决定 pagePendingStatusActivities（现在没有调用方，要么删要么用）。
5. 前端那批（34 个页面 import、菜单 SQL 路径、4 个空壳 Controller、9 个模块前缀）按冲刺计划第一周的排法走。
6. 43 处未提交改动分主题提交，别攒一个大提交。
7. 话术对齐（重要）：状态推进按"受控倒退 + 清报名"新版讲；状态机按"评估取舍"讲；付费讲设计时记得代码已落地但测试在补；数字用这份文档里核过的（57 Controller / 31 业务表 / 178 用例）。

---

最后一句：这份是题库地图，不是稿子。哪块想深聊，就把对应那节的"现在代码的真实样子"再对着代码过一遍——面试里最有说服力的永远是你记得住细节的那部分。
